# LoadBalancer.Api (.NET 9)

- Least-loaded selection based on active requests + optional CPU/MEM weights
- Health probes with automatic re-inclusion
- Automatic failover and retries
- Request/routing/health logging

## Run
```
dotnet restore
dotnet run
```
